{
    "app_dir": "/",
    "build_command": "bun run build",
    "output_dir": "dist",
    "install_command": "bun install",
    "start_command": "nginx -g 'daemon off;'",
    "framework": "docker",
    "env": {
        "VITE_POCKETBASE_URL": "https://pocketbase.zeabur.internal:8090"
    }
}